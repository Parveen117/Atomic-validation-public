# C05 Chromium Neel cut-square adapter and sharp beta burden

## Purpose

C05 replaces unguided peak hunting with the proof architecture already used by the Recognition Kernel Framework and the completed-Weil endpoint paper.

The reusable theorem is not a material-specific number. It is the exact implication

```text
complete observer + positive metric + frozen target
-> sharp minimum decoder burden beta
-> positive cut defect iff beta <= 1.
```

For chromium, the physical quantities that must be constructed are the same-specimen observer `A_Cr`, the positive response metric `H_Cr`, and the Neel boundary functional `L_N`.

## Framework pins

C05 pins commit

```text
97ab70eb3663840aa5161ccddcbb70fafe75a80b
```

of `Parveen117/Recognition-Kernel-Framework`, branch `agent/thermodynamic-cut-square-theorem`.

The consumed theorem roles are:

- thermodynamic cut-square identity and sharp decoder burden;
- bilateral jet observer and beta admissibility;
- higher-layer repair of a blind target;
- cut-loop seam curvature;
- projection-defect and Gram positivity;
- the RH completed-Weil proof chain as an architecture reference only.

The RH paper is not imported as experimental evidence for chromium.

## Candidate physical cut

For each source-locked specimen, define

```text
tau = (T - TN_source_sample) / TN_source_sample.
```

The candidate involution is

```text
J_tau: tau -> -tau.
```

This is not yet an admitted physical cut. Admission requires a source-specific `TN` with uncertainty, two-sided data around `TN`, a locked specimen and protocol, and either cut covariance or a certified bilateral closure defect.

No universal chromium `TN` is substituted across samples. AC modulation is also not relabelled as a heating or cooling branch.

## Observer tower

The base same-specimen channels are

```text
Cp anomaly
drho/dT anomaly.
```

After baseline rules and dimensional whitening are frozen, the proposed tower is

```text
A_N = direct_sum from k=0 to N of Q_k D_tau^k y.
```

Higher derivatives may only be formed from admitted raw curves. A new layer is useful only if it removes a target-relevant blind direction.

## Positive metric

The response metric is denoted `H_Cr`. The preferred construction is the inverse of the shared covariance on its positive support. Compatibility-Gram whitening is allowed, but unlike physical units may not be wedged before a metric is declared.

A singular covariance requires an explicit restriction to its positive support. It is not repaired by silently adding a convenient diagonal.

## Neel target

The target functional `L_N` must be fixed before data fitting. Admissible sources include:

- an author-supplied boundary or modulation channel;
- a protocol-derived two-sided boundary functional with a predeclared window;
- an independently calibrated transition readout on the same specimen.

An abstract relation between critical exponents is not a numerical target. Selecting the largest observed peak after seeing the data is forbidden.

## Sharp burden

The physical burden is

```text
beta_Cr,N = sup |L_N x|^2 / ||A_N x||^2_HCr.
```

On the visible quotient,

```text
beta = l (A* H A)^dagger l*.
```

The exact proof gate is

```text
A* H A - L* L >= 0  iff  beta <= 1.
```

The classification is fail closed:

```text
beta_upper < 1          STRICT_NEEL_CUT_CLOSURE
beta interval hits 1    THRESHOLD_INCONCLUSIVE
beta = 1 plus zero transverse residue
                         CRITICAL_ALIGNED_CLOSURE
beta_lower > 1          BURDEN_EXCEEDS_ONE
target not visible      TARGET_BLIND_ADD_HIGHER_LAYER
cut-loop defect large   OPEN_SEAM_OR_WRONG_CUT
```

C05 includes exact rational controls with beta `1/4`, `1`, and `4`, plus a blind-target control. These test the implementation only; they are not chromium measurements.

## Native seam curvature

The framework-native curvature is

```text
R_seam = [G_even, G_odd]
       = [t^2] log(J exp(tG) J exp(tG)).
```

A raw second derivative of a response graph is not automatically this curvature. A physical adapter

```text
rho([G_even,G_odd]) = Omega_Cr
```

must still be proved.

## Result

```text
candidate cut admitted       false
observer instantiated        false
metric instantiated          false
target instantiated          false
physical beta computed       false
native curvature computed    false
```

Status:

```text
PASS_CHROMIUM_C05_CUT_SQUARE_ADAPTER_FROZEN_BETA_DATA_REQUIRED
```

The adapter is ready. No material-specific closure claim is made.

## Next stage

`C06_CHROMIUM_PRIMARY_FILE_INTAKE_RESPONSE_PACKET_AND_BETA_INTERVAL`

C06 will admit original-byte-hashed source data into the response packet, freeze `TN`, covariance, cut pairing and target formula, and then compute an outward interval for `beta_Cr,N`.
